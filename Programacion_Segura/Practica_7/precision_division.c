#include <stdio.h>

int main() {
    int numerador_agg = 7;
    int denominador_agg = 3;

    float resultado = (float)numerador_agg / denominador_agg;
    printf("Resultado de la division: %f\n", resultado);

    return 0;
}